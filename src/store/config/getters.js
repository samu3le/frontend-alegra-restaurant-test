export default {
    getMenuItems( state, ) {
        return state.menu.items
    },
    isOpenSideBar( state, ) {
        return state.isOpenSideBar
    },
    isOpenNavBar( state, ) {
        return state.isOpenNavBar
    },
}